# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/course_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root

jwt:
  secret: dev-secret-key-for-course-management-2024-very-long-string

logging:
  level:
    com.course: debug
    com.course.mapper: debug
