# NATAPP 完整配置文件
authtoken=2789714503e149f2

# 日志配置
log=stdout
loglevel=INFO

# 禁用压缩（免费版压缩有bug）
http_proxy=

# 前端隧道（主域名）
[web]
proto=http
localport=3000

# 后端API隧道（子路径转发需要付费版）
# 免费版只能有一个隧道，所以前端直接调用完整URL
